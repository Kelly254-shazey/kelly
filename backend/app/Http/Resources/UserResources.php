<?php

// Legacy/misnamed file kept for compatibility. The real `UserResource` lives in
// `app/Http/Resources/UserResource.php`. This file no longer contains a class so
// it won't conflict with PSR-4 autoloading.
